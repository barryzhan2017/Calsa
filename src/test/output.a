List map(List l, func f){
    int len = l.size();
    int i = 0;
    while (i < len){
        int tmp = f(l.get(i));
        l.set(i, tmp);
        i = i + 1;
    }
    return l;
}

int foldleft(List l, int acc, func f) {
    int len = l.size();
    int i = 0;
    while (i < len){
        acc = f(acc, l.get(i));
        i = i + 1;
    }
    return acc;
}
int main() {
    Hashtable a;
    int i = 0;
    while (i < 50) {
        i = i + 1;
        a.set(i, 100-2*i);
    }
    print(a);
    print(a.get(25));
    print(a.hasKey(31231));
    print(a.hasKey(3));
    a.remove(2);
    print(a);

    Hashtable b = a;
    print(b);
}
