int map(List l, func f){
    int len = l.size();
    int i = 0;
    while (i < len){
        int tmp = f(l.get(i));
        l.set(i, tmp);
        i = i + 1;
    }
    return 1;
}

int foldleft(List l, int acc, func f) {
    int len = l.size();
    int i = 0;
    while (i < len){
        acc = f(l.get(i), acc);
        i = i + 1;
    }
    return acc;
}
int main() {
    List a;
    a.add(1);
    a.add(2);
    a.add(3);
    func f = int b(int x) {
        return x + 10;
    };
    a.print();
    a.map(f);
    a.print();
}
